version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Running tests...
      - cat package.json
      - npm test --prefix .
artifacts:
  files:
    - '**/*'
  discard-paths: yes
